Your Gemfile lists the gem rdoc (~> 4.3) more than once.
You should probably keep only one of them.
Remove any duplicate entries and specify the gem only once (per group).
While it's not a problem now, it could cause errors if you change the version of one of them later.
Your Gemfile lists the gem redmine_extensions (>= 0) more than once.
You should probably keep only one of them.
Remove any duplicate entries and specify the gem only once (per group).
While it's not a problem now, it could cause errors if you change the version of one of them later.
Fetching https://github.com/jbox-web/gitolite-rugged.git
Fetching https://github.com/jbox-web/grack.git
The dependency tzinfo-data (>= 0) will be unused by any of the platforms Bundler is installing for. Bundler is installing for ruby but the dependency is only for x86-mingw32, x64-mingw32, x86-mswin32. To add those platforms to the bundle, run `bundle lock --add-platform x86-mingw32 x64-mingw32 x86-mswin32`.
Fetching gem metadata from https://rubygems.org/..........
Fetching gem metadata from https://rubygems.org/.
Resolving dependencies........
Bundler could not find compatible versions for gem "actionpack":
  In Gemfile:
    actionpack-xml_parser was resolved to 1.0.2, which depends on
      actionpack (>= 4.0.0, < 5)

    haml-rails was resolved to 1.0.0, which depends on
      actionpack (>= 4.0.1)

    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      actionpack (= 4.2.11.1)

    redmine_plugins_helper was resolved to 0.9.2, which depends on
      rspec-rails was resolved to 4.1.2, which depends on
        actionpack (>= 4.2)

    redmine_plugins_helper was resolved to 0.9.2, which depends on
      sass-rails (~> 5.0) was resolved to 5.0.7, which depends on
        sprockets-rails (>= 2.0, < 4.0) was resolved to 3.2.2, which depends on
          actionpack (>= 4.0)

Bundler could not find compatible versions for gem "activemodel":
  In Gemfile:
    redmine_nonproject_modules was resolved to 0.3.7, which depends on
      eac_rails_utils (~> 0.12, >= 0.12.2) was resolved to 0.15.0, which depends on
        activemodel-associations (~> 0.2) was resolved to 0.2.0, which depends on
          activemodel (< 6, >= 4.1)

    protected_attributes was resolved to 1.1.4, which depends on
      activemodel (>= 4.0.1, < 5.0)

    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      activemodel (= 4.2.11.1)

    redmine_remotes was resolved to 0.15.1, which depends on
      validate_url (~> 1.0, >= 1.0.8) was resolved to 1.0.15, which depends on
        activemodel (>= 3.0.0)

Bundler could not find compatible versions for gem "activerecord":
  In Gemfile:
    redmine_nonproject_modules was resolved to 0.3.7, which depends on
      eac_rails_utils (~> 0.12, >= 0.12.2) was resolved to 0.15.0, which depends on
        activemodel-associations (~> 0.2) was resolved to 0.2.0, which depends on
          activerecord (< 6, >= 4.1)

    redmine_events_manager was resolved to 0.4.11, which depends on
      delayed_job_active_record was resolved to 4.1.7, which depends on
        activerecord (>= 3.0, < 8.0)

    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      activerecord (= 4.2.11.1)

    test_after_commit (~> 0.4.2) was resolved to 0.4.2, which depends on
      activerecord (>= 3.2)

Bundler could not find compatible versions for gem "activesupport":
  In Gemfile:
    redmine_nonproject_modules was resolved to 0.3.7, which depends on
      eac_rails_utils (~> 0.12, >= 0.12.2) was resolved to 0.15.0, which depends on
        activemodel-associations (~> 0.2) was resolved to 0.2.0, which depends on
          activesupport (< 6, >= 4.1)

    redmine_events_manager was resolved to 0.4.11, which depends on
      delayed_job_active_record was resolved to 4.1.7, which depends on
        delayed_job (>= 3.0, < 5) was resolved to 4.1.10, which depends on
          activesupport (>= 3.0, < 8.0)

    notifyme was resolved to 0.4.10, which depends on
      eac_ruby_utils (~> 0.46) was resolved to 0.95.1, which depends on
        activesupport (>= 4, < 7)

    haml-rails was resolved to 1.0.0, which depends on
      activesupport (>= 4.0.1)

    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      activesupport (= 4.2.11.1)

    rails-dom-testing was resolved to 1.0.9, which depends on
      activesupport (>= 4.2.0, < 5.0)

    redmine_plugins_helper was resolved to 0.9.2, which depends on
      rspec-rails was resolved to 4.1.2, which depends on
        activesupport (>= 4.2)

    redmine_plugins_helper was resolved to 0.9.2, which depends on
      sass-rails (~> 5.0) was resolved to 5.0.7, which depends on
        sprockets-rails (>= 2.0, < 4.0) was resolved to 3.2.2, which depends on
          activesupport (>= 4.0)

Bundler could not find compatible versions for gem "i18n":
  In Gemfile:
    i18n (~> 0.7.0)

    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      activesupport (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
        i18n (~> 0.7)

    faker was resolved to 2.1.0, which depends on
      i18n (>= 0.7)

Bundler could not find compatible versions for gem "mail":
  In Gemfile:
    mail (~> 2.6.4)

    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      actionmailer (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
        mail (~> 2.5, >= 2.5.4)

Bundler could not find compatible versions for gem "nokogiri":
  In Gemfile:
    nokogiri (~> 1.8.1)

    redmine_remotes was resolved to 0.15.1, which depends on
      aranha-parsers (~> 0.14, >= 0.14.1) was resolved to 0.14.1, which depends on
        nokogiri (~> 1.12, >= 1.12.4)

Bundler could not find compatible versions for gem "rack":
  In Gemfile:
    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      actionpack (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
        rack (~> 1.6)

    capybara (~> 2.13) was resolved to 2.18.0, which depends on
      rack (>= 1.0.0)

    gitlab-grack was resolved to 2.0.0.pre, which depends on
      rack (~> 1.6.0)

    rack-openid was resolved to 1.4.2, which depends on
      rack (>= 1.1.0)

    rack-parser was resolved to 0.7.0, which depends on
      rack

    redmine_plugins_helper was resolved to 0.9.2, which depends on
      sass-rails (~> 5.0) was resolved to 5.0.7, which depends on
        sprockets (>= 2.8, < 4.0) was resolved to 3.7.2, which depends on
          rack (> 1, < 3)

Bundler could not find compatible versions for gem "rack-test":
  In Gemfile:
    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      actionpack (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
        rack-test (~> 0.6.2)

    capybara (~> 2.13) was resolved to 2.18.0, which depends on
      rack-test (>= 0.5.4)

Bundler could not find compatible versions for gem "rails":
  In Gemfile:
    rails (= 4.2.11.1)

    redmine_nonproject_modules was resolved to 0.3.7, which depends on
      active_scaffold (~> 3.5) was resolved to 3.5.5, which depends on
        rails (>= 4.0.5, < 5.2.0)

    redmine_nonproject_modules was resolved to 0.3.7, which depends on
      eac_rails_utils (~> 0.12, >= 0.12.2) was resolved to 0.15.0, which depends on
        rails (>= 4.2.11)

    redmine_crm was resolved to 0.0.57, which depends on
      rails

    redmine_extensions was resolved to 0.4.4, which depends on
      rails (>= 4.2, < 6.2)

Bundler could not find compatible versions for gem "rails-dom-testing":
  In Gemfile:
    rails-dom-testing

    redmine_nonproject_modules was resolved to 0.3.7, which depends on
      jquery-rails (~> 4.4) was resolved to 4.5.0, which depends on
        rails-dom-testing (>= 1, < 3)

Bundler could not find compatible versions for gem "railties":
  In Gemfile:
    haml-rails was resolved to 1.0.0, which depends on
      railties (>= 4.0.1)

    redmine_nonproject_modules was resolved to 0.3.7, which depends on
      jquery-rails (~> 4.4) was resolved to 4.5.0, which depends on
        railties (>= 4.2.0)

    redmine_tasks_scheduler was resolved to 0.3.1, which depends on
      tasks_scheduler (~> 0.6) was resolved to 0.6.10, which depends on
        js-routes (~> 1.4, >= 1.4.14) was resolved to 1.4.14, which depends on
          railties (>= 4)

    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      railties (= 4.2.11.1)

    roadie-rails (~> 1.1.1) was resolved to 1.1.1, which depends on
      railties (>= 3.0, < 5.1)

    redmine_plugins_helper was resolved to 0.9.2, which depends on
      sass-rails (~> 5.0) was resolved to 5.0.7, which depends on
        railties (>= 4.0.0, < 6)

Bundler could not find compatible versions for gem "request_store":
  In Gemfile:
    request_store (= 1.0.5)

    redmine_nonproject_modules was resolved to 0.3.7, which depends on
      active_scaffold (~> 3.5) was resolved to 3.6.10, which depends on
        request_store (~> 1.3)

Bundler could not find compatible versions for gem "roadie":
  In Gemfile:
    roadie (~> 3.2.1)

    roadie-rails (~> 1.1.1) was resolved to 1.1.1, which depends on
      roadie (~> 3.1)

Bundler could not find compatible versions for gem "sprockets":
  In Gemfile:
    redmine_plugins_helper was resolved to 0.9.2, which depends on
      sass-rails (~> 5.0) was resolved to 5.0.7, which depends on
        sprockets (>= 2.8, < 4.0)

    redmine_plugins_helper was resolved to 0.9.2, which depends on
      sass-rails (~> 5.0) was resolved to 5.0.7, which depends on
        sprockets-rails (>= 2.0, < 4.0) was resolved to 3.2.2, which depends on
          sprockets (>= 3.0.0)

Bundler could not find compatible versions for gem "sprockets-rails":
  In Gemfile:
    rails (= 4.2.11.1) was resolved to 4.2.11.1, which depends on
      sprockets-rails

    redmine_plugins_helper was resolved to 0.9.2, which depends on
      sass-rails (~> 5.0) was resolved to 5.0.7, which depends on
        sprockets-rails (>= 2.0, < 4.0)
